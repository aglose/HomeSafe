package com.meticulouscreations.homesafe.ui.components

import com.meticulouscreations.homesafe.PlatformContext

/** The engine lives on the Swift side and is initialised by the bridge on its first peer; nothing to do here. */
actual fun warmUpLivePlayback(context: PlatformContext) = Unit
