// Types the Compose compiler treats as stable even though it can't infer it (third-party or
// cross-module classes with no @Immutable/@Stable). Everything listed here must be effectively
// immutable — a mutable type here silently disables recomposition for the state it carries.
// Read by shared/build.gradle.kts via composeCompiler.stabilityConfigurationFiles.
kotlinx.datetime.*
kotlin.time.Instant
kotlin.time.Duration
